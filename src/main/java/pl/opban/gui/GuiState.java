package pl.opban.gui;
import java.util.*;
public class GuiState {
    public static final Map<UUID, String> target = new HashMap<>();
    public static final Map<UUID, Integer> days = new HashMap<>();
}
